name: DAST Scan

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  dast:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build app image
        run: docker build -t pr7-app ./PR7/app

      - name: Start app container
        run: |
          docker run -d --name pr7-app -p 5000:5000 pr7-app
          sleep 15
          docker ps
          docker logs pr7-app || true

      - name: Check app availability
        run: curl -I http://127.0.0.1:5000 || true

      - name: Add host mapping for ZAP
        run: echo "127.0.0.1 host.docker.internal" | sudo tee -a /etc/hosts

      - name: Run ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.14.0
        with:
          target: "http://host.docker.internal:5000"
          cmd_options: "-a"

      - name: Upload ZAP report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: |
            report_html.html
            report_json.json
            report_md.md

      - name: Stop app container
        if: always()
        run: docker rm -f pr7-app || true
